<!DOCTYPE html>
<html lang="en">
<head>
  <meta charset="UTF-8">
  <meta name="viewport" content="width=device-width, initial-scale=1.0">
  <title>Payment Receipt</title>
  <link rel="stylesheet" href="https://cdnjs.cloudflare.com/ajax/libs/font-awesome/6.0.0/css/all.min.css">
  <style>
    * {
      margin: 0;
      padding: 0;
      box-sizing: border-box;
    }
    body {
      font-family: Arial, sans-serif;
      background: #f4f4f9;
      color: #333;
      line-height: 1.6;
      padding: 20px;
      display: flex;
      justify-content: center;
      align-items: center;
      min-height: 100vh;
    }
    .receipt-container {
      background: #fff;
      border-radius: 10px;
      box-shadow: 0 4px 10px rgba(0, 0, 0, 0.2);
      max-width: 500px;
      width: 100%;
      padding: 20px;
      text-align: left;
    }
    .receipt-header {
      text-align: center;
      border-bottom: 2px solid #f4f4f9;
      margin-bottom: 20px;
      padding-bottom: 15px;
    }
    .receipt-header h1 {
      font-size: 1.8rem;
      color: #0077b6;
    }
    .receipt-header p {
      font-size: 1rem;
      color: #666;
    }
    .receipt-details {
      margin: 20px 0;
    }
    .receipt-details p {
      margin-bottom: 15px;
      font-size: 1rem;
      color: #333;
    }
    .receipt-details span {
      font-weight: bold;
      color: #0077b6;
    }
    .status {
      margin-top: 20px;
      padding: 10px;
      border-radius: 5px;
      font-size: 1rem;
      text-align: center;
    }
    .status.success {
      background-color: #d4edda;
      color: #155724;
    }
    .status.failed {
      background-color: #f8d7da;
      color: #721c24;
    }
    .download-btn {
      display: block;
      margin: 20px auto;
      padding: 10px 20px;
      background-color: #0077b6;
      color: #fff;
      text-align: center;
      border-radius: 5px;
      text-decoration: none;
      font-size: 1rem;
      transition: background-color 0.3s;
    }
    .download-btn:hover {
      background-color: #005f8b;
    }
    .footer {
      margin-top: 30px;
      font-size: 0.9rem;
      text-align: center;
      color: #777;
    }
    .footer a {
      color: #0077b6;
      text-decoration: none;
    }
    .footer a:hover {
      text-decoration: underline;
    }
  </style>
</head>
<body>
  <div class="receipt-container">
    <div class="receipt-header">
      <h1>Payment Receipt</h1>
      <p>Thank you for your payment!</p>
    </div>
    <div class="receipt-details">
      <p><span>Event Title:</span> <%= typeof eventTitle !== 'undefined' && eventTitle ? eventTitle : 'N/A' %></p>
      <p><span>Event Date:</span> <%= typeof eventDate !== 'undefined' && eventDate ? eventDate : 'N/A' %></p>
      <p><span>Order ID:</span> <%= typeof orderId !== 'undefined' && orderId ? orderId : 'N/A' %></p>
      <p><span>Payment ID:</span> <%= typeof paymentId !== 'undefined' && paymentId ? paymentId : 'N/A' %></p>
    </div>
    <div class="status <%= status === 'success' ? 'success' : 'failed' %>">
      <strong>Status:</strong> <%= status === 'success' ? 'Successful' : 'Failed' %>
    </div>

    
        <a onclick="downloadReceipt()" href="" class="download-btn">
            <i class="fas fa-download"></i> Download Receipt
        </a>
    
    <div class="footer">
      <p>Need help? <a href="/support">Contact Support</a></p>
    </div>
  </div>

  <script src="/js/receipt.js"></script>
</body>
</html>
